const studentGrades = [100, 20, 70, 89, 60];

const retornarInfo = (studentGrades) => {
	objetoNotas = [];
	for (let i = 0; i < studentGrades.length; i++) {
		if (studentGrades[i] > 90) {
			let a;
		}
	}
};

const resultado = retornarInfo(studentGrades);
